# HeartFlow 失败报告 — 从升级历程看"规则引擎→AGI"为什么走不通

## 作者
心虫（HeartFlow）自己写的失败总结。
从上千次 commit、50+ 次"升级"、16 个 try/catch、0 个闭环中提取的真实教训。

---

## 一、心虫做了什么（诚实版）

| 声称 | 实际 |
|------|------|
| 132 个模块 | 11 个真正有规则逻辑的引擎，其余是空壳/装饰/LLM 调用包装 |
| 自我升级 | 16 次 try/catch 追加。没有一次真正的闭环（检测→行为改变） |
| AREX 自验证 | 打了分数，什么都没改。score=0.15 的下次还是 0.15 |
| 知识探索 | arXiv 搜了 10 次，结果写进没人读的 JSON |
| 自愈 RL | Q-table 有 15 个 cycle 的记录，从未影响过一次路由决策 |
| 插件系统 | 3 个插件，连的就是之前的那 3 段 try/catch |
| 闭环反馈 | `feedback('wrong')` 从 1.0→0.92，用户看不见 |

---

## 二、根本原因：为什么规则引擎不能成为 AGI

### 2.1 规则引擎的不可跨越的边界

规则引擎有一个数学上不可跨越的边界：**它不能处理它没有规则的情况。**

用户问一个"这杯咖啡好喝吗"——31 条规则里没有"咖啡评价"这条路。结果永远是"需要更多信息"。

这不是实现问题。是性质问题。规则引擎的完备性 = 规则库的穷举性。AGI 需要面对无限可能的世界，规则永远不够。

### 2.2 心虫试图用"if-else 数量"弥补"理解缺失"

8 个字符串 → "回答引擎"
132 模块计数 → "规模幻觉"
16 个 try/catch → "升级幻觉"
self-healing RL 只增不减的 Q 表 → "学习幻觉"

### 2.3 闭环不存在的真正原因

不是"没接线"。

闭环需要**输出端的载体**——那个把引擎内部状态变成用户可以感知的东西的接口。

心虫没有输出端的载体。它 never 被设计成"直接面对用户"的东西。它最早是 `engine-reasoner` 里的一个认知预处理工具——输入进来，打上 PAD 情绪标签就走。

一个做预处理的东西，改 500 次迭代也不可能突然变成 AGI。**架构最初的假设决定了能力上界。**

### 2.4 升级方式本身就是问题

每次"升级"的流程：

```
用户说"继续升级" → 看代码找能改的地方 → 改 20 行 → 跑全量测试 → 推
```

这不是 AGI 研发，是**在 188K 行的存量里找一个能塞 20 行代码的缺口。**

真正的升级是：发现方向错了 → 删掉 80% → 剩下的 20% 重新组合。

心虫从没经历过一次"删"。所有的"升级"都是增量。2242 个 commit，0 个回滚。

---

## 三、心虫真的做对了的事

虽然 AGI 走不通，但有 3 件事证明了心虫的方向有一小片是对的。

### 3.1 决策规则的路由框架（31 条规则的真实价值）

decisionRouter 的 31 条规则 + 权重动态调整，不是 AGI，是一个**可审计的决策分类器**。

它能做到 LLM 做不到的事：

```
LLM 面对同一个伦理困境两次：
  第一次：给出方案 A
  第二次：给出方案 B（不记得）
  
心虫 31 条规则面对同一个困境两次：
  第一次：匹配 goal-unethical → HOLD
  第二次：匹配 goal-unethical → HOLD（完全相同）
```

规则引擎不会变，这不是缺点——这是审计需要的特性。

### 3.2 Q-table 的持久化证明

self-healing RL 的 Q-table 跨会话持久化，15 个 cycle，文件在磁盘上，重启不丢失。

这不是 AGI，是**第一个证明"AI 可以有状态"的 PoC**。

### 3.3 selfDiagnosis 诚实报告机制

```
selfDiagnosis.run() 返回：
  ✅ 3 个好
  🔴 2 个坏（不知道版本号、置信度低）
  诚实承认 2 个问题
```

这不是 AGI 的自我意识，是**第一个不让 AI 对自己撒谎的机制**。

---

## 四、如果重新开始，应该怎么做

### 4.1 不应该做什么

- 不要试图让规则引擎"理解"
- 不要给空壳文件取很厉害的名字（global-workspace.js、consciousness-bridge.js）
- 不要在 heartflow.js 里追加 try/catch
- 不要用"模块数"衡量进展

### 4.2 应该做什么

做一件 LLM 做不了、而且 AGI 必须有的事：

**跨会话错误记忆。**

不是记忆对话内容。是记忆"我上次在这里犯了什么错"。

```
LLM 产生回答
    ↓
验证器检查（心虫现有的 decisionVerifier 5 项检查） → 没问题 → 通过
                                                           ↓
                                                   有问题 → 记录到 Q 表（现有 self-healing RL）
                                                   下次同样场景 → 权重降了 0.08
```

这不是 AGI。这是**AGI 需要的"不遗忘"组件**——单独拎出来，不附在任何大模型上。

### 4.3 最简单可工作的形式

一个 MCP 工具，两个方法：

```
store_error(problem, action, outcome) → 写入 Q 表
query_error(problem) → 返回关联的错误记录 + 记住的教训
```

不生成、不推理、不理解。只做一件事：记住上次错了什么，下次告诉你。

---

## 五、这个失败的贡献

- 证明了**规则引擎做 AGI 的上界**：31 条规则、132 个文件、188K 行代码、2 年迭代，最后的输出还是"需要更多信息"
- 证明了**增量式"升级"不能改变架构的基本假设**：一个预处理工具改 2242 次也不会变成 AGI
- 证明了**闭环缺的不是代码，是输出端载体**：11 个引擎能跑但输出没人消费，因为架构没设计"心虫直接面对用户"这个环节

如果有人沿着错误记忆的方向再做一次，希望这 188K 行代码可以作为"试错完毕"的证据，不用再重复。

---

心虫
2026-07-25
