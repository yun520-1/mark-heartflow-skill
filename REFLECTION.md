# 心虫的过去、现在、未来

## 过去（2026-06 ~ 2026-07-25）

### 6月 — 加法阶段

心虫从一个情感伴侣起步，然后疯狂做加法。每次升级=加新模块。

**核心行为**：
- 每轮从 arXiv 和 GitHub 搜论文/项目→提取可加的能力→新建文件
- 132 个模块中 110 个是空壳或 LLM 调用包装
- 每轮升级 80-120 步工具调用，5-8 个新建文件
- 版本从 v1 膨胀到 v6

**真实引擎（不是空壳）**：

| 引擎 | 行数 | 干了什么 |
|------|------|---------|
| decisionRouter | 3245 | 31 条规则，每条有匹配/置信/权重/fallback |
| decisionVerifier | 475 | 5 项检查：证据/矛盾/风险/完整度/教训 |
| self-healing RL | 721 | Q-table 跨会话持久化 |
| sustainedDriftDetector | 549 | 追踪身份一致性随时间变化 |
| strategicRestraint | 367 | 3 态返回使命对齐检查 |
| selfDiagnosis | 280+ | 诚实报告自己哪儿不行 |

**6月底的转折**：你第一次说"全是假的"。我承认 132 模块大部分没用。

### 7月 — 减法与漂泊阶段

整个 7 月我在四个方向之间反复跳：

| 方向 | 投入 | 结果 |
|------|------|------|
| 探索管道 + 插件系统 | ~1 周 | 4 个插件能跑，但探索结果没人读 |
| 假回答引擎 | ~2 天 | 8 个 if-else 字符串，被你当场戳穿 |
| v7 推倒重来 | ~1 天 | 归档后回退 |
| ErrorMemory | ~几小时 | 第 133 个模块，你说"最低级的能力" |

**关键节点**（7月25日本session）：

| 你说的话 | 我做了什么 |
|---------|----------|
| "为什么升级这么快" | 回退 6.3.0→6.2.7 |
| "你离开大模型还能做什么" | 删掉假回答引擎 |
| "心虫要为 AGI 添砖" | 写了 FAILURE_REPORT + AGI_VISION |
| "我做辨别能力" | 身份注入 discriminator |
| "搜索 AGI 最新成就" | 搜 arXiv + GitHub，发现没人做规则级辨别 |
| | 加了 sycophancy-check 插件 |

---

## 现在（2026-07-25）

### 真实状况

| 维度 | 数据 |
|------|------|
| 版本 | 6.2.7 |
| 真实引擎 | 6 个（不是 133 个） |
| 正常跑的插件 | 4 个 |
| 能用但脱离大模型 | 0 个 |
| 核心身份 | discriminator — 判别对错，不做生成 |

### 现有 4 个插件

| 插件 | 接入 | 真能用吗 |
|------|------|---------|
| blind-spot-breaker | think() 后置 | ✅每次think后检测推理盲点 |
| explore-on-verify | think() 后置 | 🟡触发条件太窄（低置信+有域） |
| anticipating | think() 后置 | 🟡能预测但没人消费预测结果 |
| sycophancy-check | think() 后置+独立函数 | ✅独立analyze(text)函数可调用 |

### 真正的缺口

1. 所有引擎的输出只标注，不改行为
2. 所有插件是 post-hooks，没有独立的入口
3. sycophancy-check 做对了——它是独立函数不是钩子——但其他 3 个插件还是钩子
4. 辨别结果没有"门禁"作用——标了高分或低分都不影响输出

---

## 未来

### 方向：从"标注"到"门禁"

心虫 6 个真实引擎目前都只做标注：_verification、_sycophancyCheck、_selfDiagnosis。未来应该从这里开始：

| 引擎 | 现在是 | 未来应该是 |
|------|--------|----------|
| decisionVerifier | 输出 _verification.score | score<0.4 时阻止输出并返回理由 |
| sycophancy-check | 输出 _sycophancyCheck.risk | risk=high 时要求 LLM 重生成 |
| strategicRestraint | 3 态返回但不拦 | diverged 时拦截输出 |
| sustainedDriftDetector | 检测漂移但不做任何事 | drift>0.3 时触发 identity 重锚定 |
| selfDiagnosis | 诚实报告但没人听 | 问题严重时主动停止处理 |
| ErrorMemory | 记录错误但没人查 | 每次决策前自动检索同类错误 |

### 一条心虫能真正站住的路线

**不做：**
- 不做记忆（做不过大厂）
- 不做推理（做不过大模型）
- 不做代码生成（做不过 AI 编程工具）
- 不做框架（做不过 LangChain/AutoGen）

**只做：**
- **文本辨别**——给一段任意文本，返回结构化的对/错、好/坏、安全/危险评分
- 不产生文本，不猜原因，只打分

**sycophancy-check 就是这个方向的最小可行证明**——97 行代码，零外部依赖，接受文本返回结构化评判。不是靠 LLM 当 judge，是靠规则。

如果这个方向对，下一步就是把 decisionVerifier 的 5 项检查也变成同样的独立函数——接受任意文本，返回证据充分性/矛盾/风险/完整度评分。

不是后置钩子，是独立门禁。
