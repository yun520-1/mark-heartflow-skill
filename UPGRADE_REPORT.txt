=== HeartFlow 升级报告 ===
版本: 2.6.10 → 2.6.11
日期: 2026-06-07
模块: src/utils/atomic-write.js (1057B → 12.3KB)

== 升级内容 ==

目标：atomic-write.js — 最小的 HeartFlow 模块（1057字节），仅包含一个单函数 atomicWrite，无错误分类、无重试、无验证、无批量操作。

升级内容：
1. ErrorType 枚举（9种）：WRITE_ERROR / RENAME_ERROR / VERIFY_ERROR / INVALID_INPUT / BACKUP_ERROR / DIRECTORY_ERROR / PERMISSION_ERROR / DISK_FULL / UNKNOWN
2. classifyError() 错误分类引擎：基于 err.code + err.message 双通道分类
3. getRetryDelay() 指数退避延迟计算：100ms → 300ms → 700ms（含 0-50% 随机抖动）
4. shouldRetry() 重试决策：3次上限，不可重试错误（INVALID_INPUT/DIRECTORY_ERROR）跳过
5. getFallbackPaths() 多路径回退：同目录 → 系统 tmp → 用户 home，三级自动降级
6. verifyWrite() 写入后读取验证：内容一致性检查 + 结构化 VerifyResult
7. createBackup() 写入前备份：自动备份到 .bak 后缀
8. atomicWriteJson() 原子 JSON 写入：自动 JSON.stringify + pretty-print
9. batchAtomicWrite() 批量操作：多文件并发写入，返回汇总统计
10. writeWithBackup() 带轮转备份写入：maxBackups=5 版本化备份，自动轮转
11. _stats 统计追踪：writes / retries / failures / fallbackWrites / backupFailures
12. getStats() / resetStats() 统计查询/重置
13. 向后兼容：失败时仍 throw Error（err.result 附带结构化结果）
14. 静默模式：options.throw=false 返回 { ok: false } 不抛异常

== 测试结果 ==
- 语法检查: PASS
- 加载/导出: PASS（14个导出）
- 基本写入: PASS
- JSON 写入: PASS
- 批量写入: PASS
- 写入验证: PASS
- 备份轮转: PASS
- 向后兼容（throw）: PASS
- 静默模式（throw:false）: PASS
- 统计追踪: PASS

== 影响范围 ==
atomicWrite 被 8 个模块引用：heartflow.js / meta-engine.js / external-verifier.js / experience-replay.js / reflector.js / memory/slots.js / self-healing-rl.js / memory.js
向后兼容：所有现有调用者无需修改（失败时仍 throw）
