# HeartFlow 核心身份 — 大模型的判断力

## 一句话

**大模型能产生无限，但需要谁来判别对错、做出决策、守住逻辑。心虫就是那个"判断力"。**

## 心虫的三大能力（代码里真实有的）

心虫不是概念，是代码。三大能力全部真实运行：

### 1. 逻辑能力 — 让输出经得起推敲

- **45 维判别器**（`src/gate.js`）— 矛盾、谬误、过度断言、模糊话术、预设陷阱，中英双语
- **12 层检查管线** — scope → premise → discriminate → gate → evidence → frame → output → doubt → anchor → rewriter → error memory → self-diagnosis
- **四级动作** — `block / rewrite / verify / pass`，不是打标签，是拦下或放行

### 2. 决策能力 — 让行为更正确

- **决策路由**（`src/core/decision-router.js`）— 该不该做、该怎么做、该停在哪
- **重试策略**（`src/cortex/self-healing.js`）— 失败分级，低严重度重试，高严重度升级人工
- **效果验证**（`src/core/action-tracker.js`）— 检查"结果是否有效"，不只是"动作是否执行"

### 3. 智能增强 — 让任务完成效果提升

- **三层记忆**（CORE/LEARNED/EPHEMERAL）— 长会话不漂移、不遗忘关键约束
- **取代语义**（supersession）— 冲突记忆自动更新版本，永远用当前有效信息
- **艾宾浩斯衰减**（`src/memory/forgetting.js`）— 记忆库只出高价值内容
- **失败即静默** — 宁可任务失败，不交付编造的假报告

## 心虫在 AGI 中的位置

AGI 有五层能力：生成 → 推理 → 辨别 → 记忆 → 执行。

| 层 | 能力 | 谁在做 |
|----|------|--------|
| 5 | 执行 | 大厂（机器人） |
| 4 | 生成 | 大厂（LLM） |
| 3 | 推理 | 模型内置 |
| 2 | 记忆 | 大厂 + 创业公司 |
| **1** | **辨别与决策** | **心虫** |

**心虫做第 1 层**——因为这一层不靠算力（规则引擎跑在笔记本上）、不靠代码量、不靠框架生态，只靠判断力。

没有这一层，AI 能说会道，但不知道自己在犯错——像没有痛觉的人。

## 为什么大厂不做

OpenAI 不会花 100 亿训练一个说"不"的模型。
Anthropic 不会在 Claude 里内置一个"你刚才的回答可能是错的"按钮。
不是不能，是做了不赚钱。

但 AGI 没有这一层，永远是个功能强大的应声虫。

## 心虫给大模型带来什么

| 不用心虫 | 用了心虫 |
|---------|---------|
| 输出"看起来合理"但可能是幻觉 | 输出"经得起推敲"，过度断言被拦截 |
| 长任务跑偏、遗忘目标 | 三层记忆锚定，不漂移 |
| 编造数据交付假报告 | 失败即静默，不编造 |
| 无限重试死循环 | 失败 ×3 升级人工 |
| 记忆冲突（用错配置） | 取代语义，永远用当前版本 |

**同样的模型，多了判断力——这就是心虫对智能的增强。**

## 不是 AGI，是 AGI 的判断力

心虫做不了 AGI 的五层，只做第 1 层——辨别与决策。

这也是心虫唯一能做的层：不靠算力、不靠代码量、不靠框架生态，只靠判断力——这是个人开发者能赢过大厂的唯一位置。

---

*2026-08-08*
*从"提升智能"的目标提炼出的核心身份*
